 // Multiply
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // should equal 62

// Random number greater than 0
const random = Math.floor(Math.random() * 100) + 1;

// Modulo to get remainder 4
const num3 = 16;
const num4 = 6;
const mod = num3 % num4; // should equal 4

// Find max number
const max = Math.max(1, 3, 5, 10, 20);